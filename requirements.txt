# LinuxCNC INI Configuration Editor - Python Dependencies
# Install with: pip install -r requirements.txt

# ─── GUI Framework ────────────────────────────────────────────────────────────
# PySide6 is the official Qt6 Python binding by The Qt Company.
# Provides all GUI widgets: QMainWindow, QTabWidget, QGridLayout, QLineEdit, etc.
PySide6>=6.4.0

# ─── Standard Library (no install needed) ─────────────────────────────────────
# The following modules are used but ship with Python 3.8+:
#   configparser  – secondary INI parsing for single-value sections
#   logging       – debug/info/error log output to file and console
#   shutil        – backup file creation (shutil.copy2)
#   os, sys       – file system access, environment variables, exit codes
#   pathlib       – Path object for cross-platform file paths
#   datetime      – timestamp generation for backup filenames
#   traceback     – full stack-trace logging on exceptions
